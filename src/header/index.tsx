import React from "react";

import { Container } from "./styles";

const Header = (): JSX.Element => {
    return (
        <Container>
            <p>Header</p>
        </Container>
    );
};

export default Header;
